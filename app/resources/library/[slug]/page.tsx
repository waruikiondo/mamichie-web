import React from "react";
import { supabase } from "@/lib/supabase";
import Link from "next/link";
import { ChevronLeft, Calendar, ArrowRight } from "lucide-react";
import ReactMarkdown from "react-markdown";

export const dynamic = 'force-dynamic';

const getImageUrl = (slug: string) => {
  const map: Record<string, string> = {
    "menopause-weight-gain-decoded-how-to-fight-back": "/meno.avif",
    "daily-nutritional-caloric-intake-recommendations": "/nutritional.avif",
    "lets-talk-brain-fog-understanding-menopause-mental-haze": "/brain.avif",
    "the-mamichie-3r-method-program": "/mamichie.avif",
    "fueling-the-change-the-power-of-nutrition-during-menopause": "/change.avif",
    "daily-reset-ritual-guide": "/reset.avif",
    "how-cortisol-could-be-sabotaging-your-midlife-health": "/cortisol.avif",
    "stress-your-ovaries-what-every-woman-needs-to-know": "/stress.avif",
  };
  return map[slug] || "/logo.avif";
};

interface PageProps { params: Promise<{ slug: string }>; }

async function getArticleData(slug: string) {
  const { data, error } = await supabase
    .from("posts")
    .select("title, content, pillar, published_at, summary")
    .eq("slug", slug)
    .single();
  return error ? null : data;
}

export default async function ClinicalArticlePage({ params }: PageProps) {
  const resolvedParams = await params;
  const article = await getArticleData(resolvedParams.slug);

  if (!article) return <div>Article not found.</div>;

  return (
    <article className="min-h-screen bg-[#FDFBF7] text-[#4A1D36] py-16 px-4 md:px-8 font-body">
      <div className="max-w-3xl mx-auto">
        
        {/* Navigation */}
        <Link href="/resources/library" className="flex items-center text-xs font-bold uppercase tracking-widest text-[#4A1D36]/60 mb-10 hover:text-[#4A1D36] transition-colors">
          <ChevronLeft className="w-4 h-4 mr-2" /> Back to Library
        </Link>

        {/* Header */}
        <header className="mb-12">
          <h1 className="text-4xl md:text-5xl font-bold text-[#4A1D36] mb-8 leading-[1.2] tracking-tight">
            {article.title}
          </h1>
          
          <div className="flex items-center space-x-6 text-xs uppercase tracking-widest text-[#4A1D36]/70 font-semibold mb-10 pb-8 border-b border-[#EBE1D9]">
            <span className="flex items-center">
              <Calendar className="w-4 h-4 mr-2 text-[#4A1D36]" /> 
              {new Date(article.published_at).toLocaleDateString('en-GB', { day: 'numeric', month: 'long', year: 'numeric' })}
            </span>
            <span className="text-[#4A1D36]">Dr. Vanessa Susana Stirzaker</span>
          </div>

          {/* Hero Image */}
          <div className="w-full aspect-[16/9] rounded-xl overflow-hidden shadow-sm border border-[#EBE1D9] mb-12">
            <img src={getImageUrl(resolvedParams.slug)} alt={article.title} className="w-full h-full object-cover" />
          </div>
        </header>

        {/* PERFECTED FORMATTING:
            By defining exact Tailwind classes for every HTML element generated by Markdown,
            we guarantee perfect spacing, indentation, and alignment.
        */}
        <div className="w-full text-[#4A1D36]/90">
          <ReactMarkdown
            components={{
              // Paragraphs: Beautiful line height and solid bottom margin
              p: ({ node, ...props }) => (
                <p className="text-lg leading-[1.8] mb-8 text-[#4A1D36]/90" {...props} />
              ),
              // Headings: Deep color, bold, with lots of top space and tight bottom space
              h2: ({ node, ...props }) => (
                <h2 className="text-3xl font-bold text-[#4A1D36] mt-16 mb-6 leading-tight" {...props} />
              ),
              h3: ({ node, ...props }) => (
                <h3 className="text-2xl font-bold text-[#4A1D36] mt-12 mb-4 leading-snug" {...props} />
              ),
              // Bulleted Lists: Indented heavily, clear spacing between items
              ul: ({ node, ...props }) => (
                <ul className="list-disc pl-8 mb-8 space-y-3" {...props} />
              ),
              // List Items: Matching text size and line height
              li: ({ node, ...props }) => (
                <li className="text-lg leading-[1.8] text-[#4A1D36]/90 pl-2" {...props} />
              ),
              // Bold text: Ensuring it pops
              strong: ({ node, ...props }) => (
                <strong className="font-bold text-[#4A1D36]" {...props} />
              ),
              // Blockquotes for emphasis
              blockquote: ({ node, ...props }) => (
                <blockquote className="border-l-4 border-[#4A1D36] pl-6 py-2 mb-8 italic bg-[#E5D3D6]/20" {...props} />
              ),
            }}
          >
            {article.content}
          </ReactMarkdown>
        </div>

        {/* Footer */}
        <footer className="mt-24 pt-12 border-t border-[#EBE1D9] text-center">
          <Link href="/resources/library" className="inline-flex items-center space-x-3 bg-[#4A1D36] text-white px-10 py-4 rounded-full text-sm font-bold hover:bg-[#6B2B4E] transition-all shadow-md">
            <span>Explore Clinical Library</span>
            <ArrowRight className="w-4 h-4" />
          </Link>
        </footer>
      </div>
    </article>
  );
}